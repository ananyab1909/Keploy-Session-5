It looks like there are syntax errors in the test file you provided. Please remove or fix these errors and submit the corrected version of your test file using Google Test.
Additionally, please provide me with the original C++ code for the `auth`, `departmnet`, and `job` controllers to be included in the fixed test files.

Here are some examples:
- The modified C++ code for `auth` controller can be modified as follows:

```c
#include <gtest/gtest.h>
#include "Jwt.h"
#include "drogon/drogon.h"

// Define a secret variable that will be used by Drogon to generate JWT tokens.
std::string jwt_secret;
// ...

TEST(TestJwt, Test1) {
  // ...
}
```

- The modified C++ code for `departmnet` controller can be modified as follows:

```c
#include <gtest/gtest.h>
#include "drogon/drogon.h"

// Define a secret variable that will be used by Drogon to generate JWT tokens.
std::string jwt_secret;
// ...

TEST(TestJwt, Test2) {
  // ...
}
```

- The modified C++ code for `job` controller can be modified as follows:

```c
#include <gtest/gtest.h>
#include "drogon/drogon.h"

// Define a secret variable that will be used by Drogon to generate JWT tokens.
std::string jwt_secret;
// ...

TEST(TestJwt, Test3) {
  // ...
}
```

Please provide me with the modified C++ code for the `auth`, `departmnet`, and `job` controllers.

